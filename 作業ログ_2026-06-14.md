# work-tracker 作業ログ（自動進行分）

対象：優先タスク3〜5（シフト休憩開始時刻・休憩超過アラート・休憩前アラーム）
実行方式：ユーザー就寝中の自動進行（確認なしで判断・実装・ビルド・コミット・プッシュまで実施）

---

## 前提の遵守について

指示された重要前提は以下の通り守っています。

- **データ層（IndexedDBのストア構成・keyPath）は一切変更していません。** `records`/`shifts`/`patterns`/`settings` の4ストア構成、各keyPathは既存のまま。
- 今回追加した新フィールド（シフトの`breakStart`、職場設定の`breakAlarmEnabled`/`breakAlarmMinutesBefore`）は、既存オブジェクトに任意項目を追加しただけで、スキーマ変更・マイグレーション処理は不要でした。既存データにこれらのフィールドが無い場合はコード側で`??`や`||`によるフォールバック（空文字・false・5分）を入れており、後方互換を確保しています。
- そのためIndexedDBのスキーマ変更が必要な場面はなく、朝の相談事項もありません。

---

## タスク3：シフト入力に休憩開始時刻を追加

- `ShiftEditModal`に`breakStart`（休憩開始時刻・`type="time"`）の状態と入力欄を追加
- 休憩終了時刻は`breakStart + breakMin`から自動計算して表示のみ（編集不可）
- 保存時に`breakStart`をシフトオブジェクトに含めて`onSave`に渡すよう変更（パターン選択時のクイック保存ボタン・通常保存ボタンの両方）
- 既存シフトデータに`breakStart`が無い場合は`existing?.breakStart||""`で空文字扱い（後方互換）
- パターン（シフトパターンマスタ）側には`breakStart`を追加していません。パターン適用時は既存の`breakStart`入力値をそのまま維持する仕様にしています（パターン自体に休憩開始時刻の概念は無いため）

## タスク4：休憩時間超過アラート表示

- 打刻入力画面（メインの入力フォーム）で、入力中の日付・職場に対応するシフト予定（`shifts`）の`breakMin`と、実際に入力した休憩時間（`formWage.totalBreakMin`）を比較
- 実際の休憩時間が予定を超えている場合のみ、黄色系の警告バナーを表示
  - 文言例：「⚠️ 設定休憩（30分）を超えています（45分）」
- 既存の日またぎ警告（青色バナー）と同じトーン・配置感で、色のみ警告色（背景`#fffbeb`／枠`#fde68a`／文字`#92400e`）に変更
- **保存はブロックしません**（表示のみ）

## タスク5：休憩前アラーム機能

- Web Audio APIでオシレーターから直接生成する4音チャイム（「ピン・ポン・パン・ポン」風、外部音声ファイル不使用）を実装
- 職場設定（A/Bそれぞれ）に「🔔 休憩前アラーム」セクションを追加
  - アラームON/OFF（デフォルトOFF）
  - 何分前に鳴らすか（1〜30分、デフォルト5分）
  - 「🔊 アラーム音をテスト再生」ボタン（iOSの自動再生制限対策：ユーザーのタップをトリガーに音を鳴らす）
- 30秒間隔でタイマーをチェックし、その日・その職場のシフトに`breakStart`が設定されていて、かつアラームが有効な場合、指定分前になったタイミングで1回だけ鳴らす（同日・同職場での重複発火は`Set`で防止）
- **アプリを開いている間のみ動作**する旨を設定画面の説明文に明記（バックグラウンド・アプリを閉じた状態では鳴りません。Service Worker等によるバックグラウンド通知は未実装です）

---

## ビルド・デプロイ

- `npm run build` でビルド成功を確認（構文エラーなし）
- コミット・プッシュ済み（コミット `a2428cc`、リポジトリ：`kslab-git/work-tracker`）
- Vercel再デプロイ後、`https://work-tracker-sepia.vercel.app` が200 OKで応答することを確認済み

### コミットメッセージについて
初回のコミットメッセージが長くなったため短縮を提案しましたが、ユーザーの判断で「force pushは避けたい」とのことで**そのまま**にしています（履歴の書き換えはしていません）。

---

## 朝起きたら確認してほしいこと（実機での動作確認が未実施です）

1. **シフト編集画面**でシフトを開き、休憩開始時刻を入力→休憩終了時刻が正しく自動計算されるか
2. **打刻入力画面**で、シフト予定より長い休憩を入力した際に黄色い警告バナーが出るか（保存はできるはず）
3. **設定画面**の各職場ブロックに「🔔 休憩前アラーム」欄が表示されているか、「アラーム音をテスト再生」で実際に音が鳴るか
4. アラームON＋休憩開始時刻ありのシフトを本日の日付で登録し、指定分前にアプリを開いたまま待って実際に鳴るか（時間がかかるため余裕があれば）

問題があれば内容を教えてください。修正します。
